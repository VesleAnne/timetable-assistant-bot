# Timetable Assistant Bot - Environment Variables
# Copy this file to .env and fill in your actual values

# Bot Tokens (Required)

# Discord Bot Token
# Get from: https://discord.com/developers/applications
# 1. Create application → Bot tab → Copy token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Telegram Bot Token  
# Get from: @BotFather on Telegram
# 1. Send /newbot → Follow prompts → Copy token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Database Configuration (Optional)

# SQLite database path
# Default: ./data/bot.db
# The directory will be created automatically if it doesn't exist
SQLITE_PATH=./data/bot.db

LM_PROVIDER=your_llm_provider
LLM_MODEL=your_model
#For Ollama only:
# OLLAMA_BASE_URL=http://localhost:11434
LLM_TIMEOUT=60 #you can change this parameter

# Notes

# Environment variables override values in configuration.yaml
# Only tokens should be in .env - other settings can stay in configuration.yaml

